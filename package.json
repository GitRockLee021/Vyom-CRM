{
  "name": "finconsul-crm",
  "version": "1.0.0",
  "private": true,
  "description": "FinConsul CRM - Accounting & finance consulting firm CRM",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client",
    "db:init": "psql -U postgres -c \"CREATE DATABASE finconsul;\" && psql -U postgres -d finconsul -f database/schema.sql"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
